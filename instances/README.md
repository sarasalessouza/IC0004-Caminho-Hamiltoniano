Instancias no formato `n m` seguido de `m` arestas `u v` (grafo não direcionado).

- `auto/`: instâncias geradas por `scripts/generate_instances.py`
- `exemplo_minimo_sim.txt` e `exemplo_minimo_nao.txt`: casos pequenos para validação.
